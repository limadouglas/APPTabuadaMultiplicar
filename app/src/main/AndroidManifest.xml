<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lima.douglas.apptabuadamultiplicar">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".MenuTreinamentoActivity"></activity>
        <activity android:name=".TelaTreinamentoActivity"></activity>
        <activity android:name=".DesafioActivity"></activity>
        <activity android:name=".TabuadaActivity"></activity>
        <activity android:name=".RecordesActivity"></activity>
    </application>

</manifest>
